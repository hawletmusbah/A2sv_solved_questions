class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        count =defaultdict(int)
        for i in nums:
            if i not in count:
                count[i]=1
            else:
                count[i]+=1
        print(count)
        for j in count:
            if count[j] == 1:
                return j 
